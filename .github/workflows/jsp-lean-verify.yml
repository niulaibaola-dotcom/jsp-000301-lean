name: JSP Lean Verify

on:
  push:
    branches:
      - main
    paths:
      - "jsp-lean/**"
      - ".github/workflows/jsp-lean-verify.yml"
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jsp-lean
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install elan
        run: |
          curl https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh -sSf | sh -s -- -y
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Fetch dependencies
        run: lake update

      - name: Restore mathlib build cache
        run: lake exe cache get

      - name: Build proof
        run: lake env lean JSP000301.lean

      - name: Reject placeholders
        run: |
          if grep -R -nE '\b(sorry|admit)\b' JSP000301.lean; then
            echo "Forbidden placeholder found"
            exit 1
          fi
